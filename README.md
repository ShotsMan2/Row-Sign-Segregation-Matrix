# Row-Sign-Segregation-Matrix
A C program that generates a mixed array of integers [-10, 10] and populates an NxN matrix such that even-indexed rows contain strictly positive numbers and odd-indexed rows contain strictly negative numbers.
